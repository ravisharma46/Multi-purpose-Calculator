<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apple.calculator">

    <application
        android:allowBackup="true"
        android:icon="@drawable/arithmetic_1029103"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".squareRoot" />
        <activity android:name=".QuadraticEquationsSolver" />
        <activity android:name=".loan_payment_calculate" />
        <activity android:name=".arithmeticOperations"></activity>
    </application>

</manifest>